"""Django app holding the persistent game-state schema (players, NPC state,
relationships, world objects)."""

from __future__ import annotations

default_app_config = "src.persistence.gamedata.apps.GamedataConfig"
